{
  "name": "quarterwindows",
  "version": "0.1.0",
  "description": "Add additional window management shortcuts",
  "main": "extension.js",
  "devDependencies": {
    "prettier": "^2.5.1"
  },
  "scripts": {
    "lint": "prettier -c extension.js",
    "lintfix": "prettier -w extension.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Troy Ready",
  "license": "GPL-2.0-or-later",
  "prettier": {
    "trailingComma": "all"
  }
}
